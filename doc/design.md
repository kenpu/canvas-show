type Point {
    :x 
    :y
}

// Some vector computation needed

    define TouchInput {
        on_raw_point(e) {
            update_slide_window(this, PointOf(e));
            when(newSegment(seg)) {
                update_
            }
        }
    }
